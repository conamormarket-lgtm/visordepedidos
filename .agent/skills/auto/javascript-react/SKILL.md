---
name: javascript-react
description: "Javascript React for visordepedidos. 1 conventions, 3 fixes."
domain: javascript-react
composesFrom:
  - react
triggers:
  - glob: "**/*.jsx"
autoGenerated: true
enabled: true
---

# Javascript React

Auto-compiled from **6 real patterns** in **visordepedidos**. This skill is auto-routed to agents when working on javascript-react files.

## 🔧 Problem Playbooks

### Fixed null crash in Reset — avoids unnecessary re-renders in React
-             const termLower = term.toLowerCase();
+             const term = searchTerm.trim();
-             const termClean = term.replace(/\s+/g, '');
+             const termLower = term.toLowerCase();
-             const isNumeric = /^\d+$/.test(termClean);
+             const termClean = term.replace(/\s+/g, '');
- 
+             const isNumeric = /^\d+$/.test(termClean);
-        

**Actionable Steps:**
1. Modified 1 files
2. identifier: Reset
3. identifier: Solo
4. identifier: Math
5. identifier: Clear

### problem-fix in ImageCarousel.jsx
File updated (external): src/components/ImageCarousel.jsx

Content summary (66 lines):
import React, { useState } from 'react';
import { convertDriveLink } from '../utils/drive';

const ImageItem = ({ img, idx }) => {
    const [failed, setFailed] = useState(false);
    const thumbnailUrl = convertDriveLink(img);

    if (failed) {
        return (
            <div className="relative rou

**Actionable Steps:**
1. Modified 1 files

### problem-fix in ImageCarousel.jsx
File updated (external): src/components/ImageCarousel.jsx

Content summary (66 lines):
import React, { useState } from 'react';
import { convertDriveLink } from '../utils/drive';

const ImageItem = ({ img, idx }) => {
    const [failed, setFailed] = useState(false);
    const thumbnailUrl = convertDriveLink(img);

    if (failed) {
        return (
            <div className="relative rou

**Actionable Steps:**
1. Modified 1 files

## 📐 Conventions & Best Practices

### Project Conventions
- 📐 **Fixed null crash in Falta — prevents null/undefined runtime crashes — confirmed 3x** — -         "Falta polo",
+         "Falta polo"
-         "Falta gorra"
+     ];
-     ];
+ 
- 

## 🔵 Architecture

### how-it-works in Layout.jsx
File updated (external): src/components/Layout.jsx

Content summary (24 lines):
import React from 'react';

const Layout = ({ children, header, footer, ...props }) => {
    return (
        <div c

### how-it-works in StockPauseAlert.jsx
File updated (external): src/components/StockPauseAlert.jsx

Content summary (17 lines):
import React from 'react';
import { AlertTriangle } from 'lucide-react';

const StockPauseAlert = ({ isPause

---
*Auto-generated by BrainSync 🧠 | 6 patterns | 2026-04-21*
